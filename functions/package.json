{
  "name": "functions",
  "description": "DOT 서버 프록시 - 텍스트 계열 Gemini 호출 중계 (A-1)",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "npm run build && node lib/rate_limit.test.js && node lib/signed_url_policy.test.js && node lib/payload_limit.test.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  },
  "private": true
}
