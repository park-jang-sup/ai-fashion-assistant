# 작업 지시서 — 자기 평가 척도 타당도·신뢰도 트랙 (task_selfeval_validity_v1)

**트랙 상태: 진행 중 (0·1·2단계 완료 — 계측 배선을 재빌드 후 실기기
데이터로 검증 통과, §4-3. 3단계 설계 사전 등록 완료(§6, 반복
일관성·사람 라벨 쌍대비교) — 착수(실행)는 아직 하지 않음.
2026-08-11).**

## 0. 이 트랙의 위치와 승계 원칙

`task_hardening_v2`(서버 접근 통제 강화 트랙)와는 별도 트랙이다 — 대상이
서버 방어가 아니라 자기 평가 척도(`OutfitSelfEvaluator`)의 타당도·신뢰도이므로
새 문서로 시작한다. 다만 **그 트랙에서 확립한 절차를 그대로 승계한다** —
판정 기준을 결과 보기 전에 사전 등록한다, 계측을 먼저 하고 고치는 것은
나중이다, 미확정을 확정으로 쓰지 않는다, **그리고 실기기 관측을 시작하기
전에 설치 시각을 대상 커밋 해시와 대조한다**(클라이언트·서버 양쪽 검증
대상의 동일성이 함께 성립해야 한다 — `task_hardening_v2.md` §2). 상세
근거·전례는 `docs/task_hardening_v2.md`를 가리키며 여기서 반복하지 않는다.

**[정정 2026-08-11] 마지막 항목(재빌드 대조)이 최초 승계 목록에서
빠져 있었다.** 그 결과 2단계 계측 배선 직후의 첫 관측(§4-1)이 배선
이전 빌드에서 나왔다 — 신 필드 4종이 전부 없는 문서를 "배선 실패"로
오판할 뻔했다(실제로는 배선은 됐고 관측이 낡은 빌드를 봤을 뿐이었다).

이는 `task_hardening_v2.md`가 이미 두 번 등록한 재빌드 절차 결함
(논문 5.20.6·5.21.7절)의 **세 번째 재발**이지만, 앞의 둘과 원인이
다르다:

- 5.20.6·5.21.7 — 규약은 이미 있었으나 그 순간 지키지 않았다
  (**준수 실패**).
- 이번 — 규약을 새 트랙 문서로 옮겨 적는 목록에서 빠졌다
  (**승계 실패**). `task_hardening_v2.md` §2가 이 규약을 "서버
  커밋 해시"에서 "클라이언트·서버 양쪽"으로 이미 확장까지 해
  두었는데, 그 확장된 형태조차 이 문서를 새로 쓸 때 옮겨지지
  않았다.

`task_hardening_v2.md` 5.22.3(이 트랙이 아니라 그 트랙의 논문 반영
절)이 등록한 "앞 단계의 분류·목록을 다음 단계가 검증 없이 물려받는다"의
변종이다 — 그쪽 네 사례는 **물려받은 것이 틀렸던** 경우였고, 이번은
**물려받아야 할 것이 통째로 빠진** 경우다. 이 차이가 발견 난이도를
가른다: 틀린 값은 대조하면 바로 드러나지만, **없는 항목은 대조할
대상 자체가 없어서** 그 항목이 실제로 필요해지는 순간(이번엔 배선
직후 첫 관측)이 되어서야 드러난다 — 그래서 누락은 승계된 오류보다
항상 늦게 발견된다.

**조치**: 위 목록에 재빌드 대조를 추가했다. 이 문서의 모든 실기기
관측 절에 "설치 시각 / 대상 커밋 해시" 기록란을 둔다(§4-1, §4-2).
앞으로 새 트랙 문서를 만들 때 이 승계 목록을 처음부터 다시 옮겨
적지 않는다 — **옮겨 적는 행위 자체가 이번 누락이 생긴 지점**이므로,
대신 `docs/task_hardening_v2.md` §0을 참조하도록 링크만 건다.

## 1. 배경 — 왜 이 트랙인가

자기 평가 점수(`OutfitSelfEvaluator`의 `[총점]`)는 자기 수리 루프 전체의
근거이며, 논문이 "에이전트"의 조작적 정의(1.2절 요건 2 — 자기 평가와 수리)를
구현했다고 서술하는 핵심 증거다(3.3절 설계, 6.2절 "65점 → 색상 원인 진단 →
하의 교체 → 75점"의 실기기 로그 인용). `threshold=70`이 수리 발동 여부를
가른다. 그런데 이 점수 자체가 **반복해도 같은 값을 내는지(신뢰도)**,
**사람의 판단과 상관이 있는지(사람 상관)**, **총점이 그 진단 근거인 세 축을
실제로 반영하는지(구성 타당도)** — 셋 다 한 번도 측정된 적이 없다.

## 2. 0단계 — 척도의 실제 구조 파악 (완료, 코드 읽기만)

측정 설계 이전에 무엇을 재는지부터 확인했다. 실행 없이 코드만 읽었다.

- **산출 경로**: 자기 평가는 `analyzeOutfitFromAttributes`의 세 프롬프트
  분기 중 항상 세 번째(`_buildAttributeAnalysisPrompt`, `gemini_service.dart:1020-1066`)만
  탄다 — 사진·프로필을 넘기지 않기 때문이다(`outfit_self_evaluator.dart`의
  `evalOne`이 `analyzeOutfitFromAttributes`를 호출할 때 `userPhotoUrl`/
  `userProfile`을 아예 넘기지 않는다).
  이 분기만 다축 형식(`[총점]`/`[격식적합]`/`[색상조화]`/`[스타일통일]`/`[개선점]`)을
  요구한다. **`[총점]`은 세 축의 합/평균/최솟값이 아니라 모델이 독립적으로
  생성하는 다섯 번째 숫자다** — 우리 코드가 총점을 세 축으로부터 계산하는
  자리는 없다. 응답은 JSON 스키마가 아니라 자유 텍스트이며 정규식으로
  파싱한다(`parseScore`, `_parseAxisScore`).
- **결정성**: `generationConfig: {temperature: 0.7, maxOutputTokens: 3000}`
  (`gemini_service.dart:677-680`) — 0이 아니다, `thinkingConfig`로 추론을
  끄지도 않는다. 반복 측정이 의미를 갖는 조건이다. 모델도 고정이 아니다 —
  기본 `gemini-3.5-flash`가 타임아웃/재시도 가능 오류로 실패하면
  `withTextModelFallback`이 조용히 `gemini-3.1-flash-lite`로 넘어간다
  (`gemini_service.dart:76-91`). 어느 모델이 응답했는지 기록하지 않으면
  반복 측정이 두 모델의 분산을 섞는다.
- **입력 구성**: 항목 속성 텍스트(`toPromptLine()`)만 들어가고 이미지·TPO·
  날씨·체형 프로필은 안 들어간다. 다만 `recentHistoryText`(취향 이력)가
  `FirestoreService.getRelevantHistorySilently`에서 매번 새로 조회되며,
  그 랭킹이 과거 자기 평가 점수(`colorScore >= 80`)에 의존한다
  (`firestore_service.dart:516`) — **점수가 미래의 입력을 바꾸는 피드백
  루프**가 있다. "같은 조합을 며칠 뒤 다시 평가"는 엄밀히는 같은 입력이
  아니다. 새 옷 등록 경로(`agent_planner.dart:859`)는 이 이력을 아예
  넘기지 않아(호출 시 인자 자체가 없음) 입력이 조합만으로 완전히
  결정된다 — 반복 측정의 vehicle로 더 적합하다.
- **`threshold=70`의 출처**: `outfit_self_evaluator.dart:96`의 평범한 Dart
  상수(`dart-define`도 정책 객체도 아니다). "실측 최저 65 반영"이라는
  기존 서술(5.8.6절)은 **70 자체의 근거가 아니라** `_lowScoreFloor`라는
  별개 상수(예전 60, 채택 기준과 따로 놀아 도달 불가능했다)를 이 값에
  맞춘 것이다. 70이 왜 처음 그 값으로 정해졌는지는 코드·문서 어디에도
  근거가 없다.
- **소비처**: `threshold` 비교(채택/수리) 외에 `Firestore(users/{uid}/recommendations)`에
  `colorScore`(=총점, 이름은 오해의 소지가 있다 — 아래 B-4)와
  `candidateScores`로 저장되고, 그 `colorScore >= 80`이 위 이력 랭킹에
  재사용된다. 읽기 전용 도구 `tools/score_distribution_report/`가 이미
  존재해 이 저장 데이터를 조회한다.

## 3. 1단계 — 기존 데이터로 답할 수 있는 것부터 (완료, 2026-08-11, 읽기 전용)

새 LLM 호출을 만들기 전에 이미 쌓인 데이터로 답할 수 있는 질문을 먼저
소진했다. `tools/score_distribution_report/score_distribution_report.py`를
그대로 실행하고, 같은 자격 증명으로 보조 조회(itemIds 조인) 하나를 추가로
돌렸다. **Firestore에 쓰지 않았고, LLM을 호출하지 않았고, 코드를 수정하지
않았다.**

### 3-1. 실측 결과 (A)

`ai-fashion-assistant-personal`, 2026-08-11 실행:

- **표본**: 66 이벤트 / 3 계정 / 72 후보 점수(대부분 이벤트가 후보 1개만
  평가하고 통과 — `bestScore` 분포와 `candidateScores` 전체 분포가
  거의 같다).
- **`bestScore`**: 최소 65, 최대 96, Q1 75, 중앙값 82, Q3 86.5, 평균 82.0,
  SD 7.1.
- **`threshold<70` 비율**: 1/66 (1.5%).
- **0 존재 여부**: 없음. 도구 소스(`score_distribution_report.py:143-144`)를
  읽어 0을 필터링하지 않음을 확인했다 — **부재가 실제다**, 도구가 감춘
  것이 아니다.
- **실효 해상도**: 65~96 구간에서 `bestScore` 기준 서로 다른 17개 값,
  전체 72개 후보 점수 기준 18개 값. 명목 척도는 1~100이지만 실제로
  관측된 폭은 32점(65~96)뿐이다.

**기록할 한계**: 호출 수준 실패(네트워크 오류·빈 응답·안전 차단 — Gemini
API 자체가 실패해 `_extractTextFromResponse`가 예외를 던지는 경우,
`outfit_self_evaluator.dart:128-132`)는 `candidateScores`에 **아예 도달하지
못한다.** "파싱 실패 0건"은 정규식 파싱(`parseScore`가 텍스트는 받았지만
`[총점]` 패턴을 못 찾은 경우)에 한정된 진술이며, 호출 자체가 실패한
빈도는 이 데이터로 알 수 없다(서버 로그·`agent_logs`의 `wasError` 이벤트
쪽에서만 관측 가능하고, 이번 조회 범위 밖).

### 3-2. 발견 4건

#### B-1. 수리 발동률 1.5% — 두 원인이 구분되지 않는다

논문은 자기 평가·수리 루프를 에이전트 요건 2의 구현 증거로 서술한다
(1.2절, 3.3절, 6.2절). 그런데 실사용 66건 중 `threshold` 미달은 1건뿐이며,
`enableRepair`가 백그라운드 경로(`agent_planner.dart:859`)에서만 켜지므로
실제 수리 **실행**은 그보다 적을 수 있다(TPO 기반 경로는 미달해도 수리를
시도하지 않고 그냥 fallback 문구만 붙인다).

두 원인이 가능하며 함의가 전혀 다르다:

- **(가) 조합 품질이 실제로 높다** — 매칭 엔진(`OutfitMatcher`)이 격식·색상
  궁합이 좋은 후보만 올려보내고 있다는 뜻. 좋은 소식이다.
- **(나) 척도가 70 아래로 잘 내려가지 않는다** — 채택 기준이 척도의 실제
  분포와 어긋나 있다는 뜻. 5.8.6절이 `_lowScoreFloor`(60)에 대해 이미
  규명한 "임계값과 실측 분포의 불일치"가 **채택 기준(threshold=70)
  자체에서 반복되는 것**이다.

**현재 데이터로는 구분되지 않는다** — 둘 다 "낮은 미달률"이라는 같은
관측을 낸다. 구분하려면 사람 라벨과의 상관이 필요하다(§5, 3단계).

이 갈림에 대해 논문이 이미 서 있는 지점이 하나 있다 — 5.8.6절은 관측
최저값 65를 두고 "자기 평가에 사용하는 대형 언어모델이 100점 척도에서
낙제점을 잘 부여하지 않는 일반적 성향과 일치한다"고 적었다. 이는 (나)
방향의 **가설**이지 **검증**이 아니다 — "일반적 성향과 일치한다"는 것과
"이 시스템에서 그 성향이 실제로 작동하고 있다"는 것은 다른 문장이다.
5.8.6절 자체가 이미 정정한 것처럼, `_lowScoreFloor`를 70으로 올려 도달
가능하게 만든 것과 70이라는 자리가 변별력 있는 자리인지는 별개 질문이며,
후자는 그때도 지금도 검토된 적이 없다.

#### B-2. 척도의 실효 해상도가 명목 척도와 다르다

명목 1~100, 실측 65~96(32점 폭), 서로 다른 값 17~18개. `threshold=70`은
실측 폭의 아래쪽 약 16% 지점(65~96 중 70은 아래에서 5/31 ≈ 16%)이다.

**이후 측정 설계는 1~100이 아니라 65~96을 전제로 한다** — 예컨대 "7점
차이"의 의미가 100점 척도에서와 32점 폭에서 다르다(전자는 7%, 후자는
약 22%). 표본 크기·효과 크기 산정을 이 실측 폭 기준으로 다시 해야 한다.

#### B-3. 축 점수는 사후 복원이 원리적으로 불가능했다 (2단계에서 해소)

`recommendation_entry.dart`(2단계 작업 전)에 축 필드가 없었을 뿐 아니라,
`outfit_self_evaluator.dart:324-330`(구 버전)의 `stripScoreLine`이 저장
**전에** `[총점]`/`[격식적합]`/`[색상조화]`/`[스타일통일]`/`[개선점]` 줄을
명시적으로 제거하고 있었다. 축 점수는 수리 판단 도중 메모리에만 존재하고
버려졌다.

따라서 구성 타당도 질문("총점이 세 축을 실제로 반영하는가")은 기존
데이터로 답할 수 없었다. **표본이 작아서가 아니라 계측 자체가 없었기
때문**이며, 표본을 늘려도 해소되지 않는 문제였다.

논문 3.3절(구현 서술, 286행)은 "총점이 기준(70점) 미만일 때 ... 세 축
중 최저 점수 축을 원인으로 진단한다"고 인과를 서술하는데, 코드상 총점은
세 축과 독립적으로 생성되는 다섯 번째 숫자다(§2). 5.8.8절이 "독립적이라
가정한 두 정책이 실제로는 의존 관계였던" 사례라면, 이번은 그 거울상이다
— **의존한다고 서술한 두 값이 실제로는 (코드상) 독립으로 생성된다.**
둘이 실제로 상관되어 있는지는 축 점수가 쌓이기 시작해야 확인할 수 있다
— §4에서 그 계측을 배선했다.

**[2026-08-11 추가] "코드상 독립"과 "모델 출력 수준에서 독립"은 다른
문장일 수 있다.** §4-3의 3건에서 총점이 축 평균보다 매번 3.3~4.7점
낮게 나왔다 — 편차가 순수 잡음이라면 부호가 갈려야 하는데, 세 건
다 같은 방향이었다. 이게 규칙성이라면, **코드가 계산 관계를 두지
않았다는 사실과 모델이 두 값을 만들 때 실제로 참고하는 관계가 있다는
사실은 양립**한다 — 우리 코드는 총점을 축으로부터 유도하지 않지만,
모델이 스스로 두 값을 낼 때 내부적으로 일관된 습관을 가질 수 있다.
**지금 이 문단으로 B-3의 결론을 정정하지 않는다** — 표본 3은 방향성을
말하기엔 너무 작다. 앞으로 이 편차를 고정 지표로 계속 관측하고,
그 결과로만 정정 여부를 판단한다(아래 "총점-축 편차 추적" 참고).

#### B-4. (부수) `colorScore` 필드명이 총점을 담고 있다

`RecommendationEntry.colorScore`는 다축 평가 이전 시대(단일 `[점수]`)의
잔재 필드명이다. 지금은 총점(`[총점]`)이 그 자리에 들어간다. 그런데
이 필드가 `firestore_service.dart:516`의 이력 관련도 랭킹(`>= 80` 필터)에
쓰인다 — 이름은 색상 점수인데 실제로는 총점으로 필터링하고 있다. 다음
사람이 필드명만 보고 읽으면 오독한다.

이번 트랙에서 이름을 바꾸지 않는다 — 기존 문서 마이그레이션이 필요하고
이 트랙의 범위가 아니다. 대신 선언부(`recommendation_entry.dart`)와
사용처(`firestore_service.dart:516`)에 주석으로 명시했다(§4에서 완료).
논문에 반영할지는 이후 판단 — 이 문서에 등록해 두는 것으로 지금은 충분하다.

## 4. 2단계 — 계측 배선 (완료 — 코드 커밋, 실기기 검증 통과. 1차 검증(§4-1)은
배선 이전 빌드를 봐 무효였고, 재빌드 후 2차 검증(§4-3)에서 확정됨)

구성 타당도(B-3)가 신뢰도보다 앞이므로, 반복 측정을 시작하기 전에 축
점수를 남기는 배선을 먼저 했다. **이번 커밋에서 반복 측정 자체는
시작하지 않는다** — 계측이 배포되고 데이터가 쌓이기 시작한 뒤에 3단계
설계를 사전 등록한다.

### 작업 1 — 축 점수 저장

`outfit_self_evaluator.dart`가 이미 수리 판단을 위해 세 축을 파싱하고
있었다(`_parseAxes`). 이제 그 값을 **모든 후보**에서 파싱하고(수리 분기
전용이 아니다 — 자기 평가 프롬프트는 항상 다축 형식이므로 총점과 함께
매번 나온다) 버리지 않고 `SelfEvalOutcome`/`RecommendationEntry`에 싣는다.

- `SelfEvalOutcome`에 `candidateFormalityScores`/`candidateColorHarmonyScores`/
  `candidateStyleScores: List<int>` 추가 — `candidateScores`와 길이·순서가
  항상 같다(승자만 남기면 총점-축 관계를 볼 표본이 이벤트당 1개로 줄기
  때문에, 탈락 후보까지 포함해 후보별로 남긴다).
- 필드명은 축 이름을 그대로 썼다 — `candidateColorHarmonyScores`처럼,
  기존 `colorScore`(총점, B-4)와 혼동되지 않는 이름을 골랐다.
- `RecommendationEntry`에 같은 세 필드를 추가하고 `fromFirestore`/
  `toFirestore` 양쪽에 배선했다.

### 작업 2 — 파싱 실패 표현 통일

기존에는 인메모리 최선-후보 비교가 `-1`, Firestore 저장이 `0`으로 서로
다른 값을 썼다. `outfit_self_evaluator.dart`의 비교 로직을 `0`으로
통일했다(저장 쪽 규약에 맞춤 — 이미 쓰이고 있던 쪽으로).

**안전한 이유**: `parseScore`가 실제 파싱값을 `[1,100]`으로 clamp하므로
진짜 점수는 절대 0이 될 수 없다 — `0`은 "파싱 실패"만을 가리키는 값으로
항상 구분된다. 비교 결과도 동일하다 — `-1`이든 `0`이든 진짜 점수의
최솟값(1)보다 항상 작으므로, 실패한 후보가 성공한 후보를 밀어내는 일은
어느 쪽 규약에서도 없다. **1단계 실측(§3-1)에서 기존 저장 데이터에 0이
한 번도 없었음이 확인되었으므로, 이 통일이 과거 데이터의 해석을 바꾸지
않는다.**

### 작업 3 — 응답 모델 기록

`withTextModelFallback`이 타임아웃/재시도 가능 오류 시 조용히 대체
모델(`gemini-3.1-flash-lite`)로 넘어간다(§2). 어느 모델이 답했는지
기록하지 않으면 반복 측정이 서로 다른 두 모델의 분산을 섞는다. 서버
로그에는 이미 있으나(`[callGeminiText] start ... model=...`) 클라이언트
저장 데이터에는 없었다 — 저장 쪽에 실었다.

구현: `evalOne`의 반환 타입을 `Future<String?>`에서
`Future<({String? text, String? model})>`로 바꾸고, `withTextModelFallback`에
넘기는 클로저가 실행될 때마다(=실제로 그 모델이 쓰일 때) 외부 변수에
모델명을 기록한다 — `withTextModelFallback` 자체(다른 5개 호출부가
공유하는 헬퍼)는 건드리지 않았다, 이번 필요는 자기 평가에 한정된다.
`SelfEvalOutcome.candidateModels: List<String>`으로 노출하고
`RecommendationEntry.candidateModels`에 저장한다.

### 작업 4 — `stripScoreLine`은 그대로 둔다

사용자에게 보이는 텍스트(`summaryText`)에서 점수 메타 줄을 제거하는 것은
의도된 동작이라 바꾸지 않는다. 작업 1이 축 점수를 구조화된 필드로 따로
저장하므로, 원문에 점수 줄을 남겨 둘 필요가 없다. 이 판단을 코드
주석으로 남겼다(`outfit_self_evaluator.dart`, `stripScoreLine` 바로 위).

### 검증

- `flutter analyze` — 0 issues.
- `flutter test` — 166/166 통과. **다만 `OutfitSelfEvaluator`/
  `SelfEvalOutcome`을 직접 겨냥한 단위 테스트는 이 저장소에 없다**
  (`test/`에 관련 파일 없음, 확인함) — 위 통과는 회귀가 없다는 뜻이지
  이번에 바꾼 동작 자체를 검증한 것은 아니다. 타입 정합성은
  `flutter analyze`가, 실제 동작은 아래 실기기 확인이 담당한다.
- **실기기 확인은 사용자 몫으로 남는다**: 새 옷 등록 1회를 실행해
  Firestore `recommendations` 문서에 `candidateFormalityScores`/
  `candidateColorHarmonyScores`/`candidateStyleScores`/`candidateModels`가
  실제로 채워지는지 확인한다. 이 문서에는 아직 그 결과가 없다 — 별도
  사건으로 기록한다.

## 4-1. 2단계 검증 (2026-08-11, 읽기 전용 조회) — 결과: 배선 미반영

**[2026-08-11 추가] 이 절이 다루는 5건의 지위를 재정의한다.** §0의
정정에서 등록했듯, 이 5건을 만들 때 설치 시각/커밋 해시 대조를
하지 않았다 — 그래서 이 5건은 **총점 관측 표본으로는 유효하지만
(아래 B, 82/82/82/75/88과 누적 71건 중 1건 미달·1.4%는 그대로 쓴다),
2단계 계측 배선의 검증 근거로는 무효다.** 배선 이전 빌드가 만든
데이터라면 신 필드가 없는 것이 당연하므로, 이 5건만으로는 "배선이
실제로 실패했다"와 "배선은 됐지만 관측이 낡은 빌드를 봤다"가
구분되지 않는다 — 아래 A의 "배선 미반영" 판정은 그 구분 이전에
내려진 **1차 판단**이며, 어느 쪽이 맞는지는 §4-2(재빌드·재설치 후
재검증)에서 확정한다. 1차 판단 자체는 §0이 등록한 "승계 실패"
사례의 직접 증거이므로 지우지 않고 그대로 둔다.

### A) 저장 확인

1단계와 같은 서비스 계정 키로 `users/{uid}/recommendations`를 최신순
조회했다(`Firestore에 쓰지 않음`). 사용자가 "옷장과 잘 안 맞는 쪽"을
의도적으로 고른 신규 5건(2026-08-11 06:50:03~06:54:24 UTC, uid
`BDDOIl08...`, 4분 21초 사이에 연속 생성)을 확인한 결과:

| doc | createdAt(UTC) | colorScore(총점) | candidateFormalityScores | candidateColorHarmonyScores | candidateStyleScores | candidateModels |
|---|---|---|---|---|---|---|
| `KDVGUHNgkRhhyqIJf1jI` | 06:54:24 | 88 | (없음) | (없음) | (없음) | (없음) |
| `5bnjjJbAoU6SRYQDhDIe` | 06:53:32 | 75 | (없음) | (없음) | (없음) | (없음) |
| `U6lin7MpBa8ageOH8zD6` | 06:52:26 | 82 | (없음) | (없음) | (없음) | (없음) |
| `FVp630ZOuZzUcSh2gTfv` | 06:51:18 | 82 | (없음) | (없음) | (없음) | (없음) |
| `7k4biDiE4mOU4HlsxjQX` | 06:50:03 | 82 | (없음) | (없음) | (없음) | (없음) |

**5건 전부 새 필드 4종이 문서에 아예 없다** — 값이 `0`이거나 빈 배열인
게 아니라 필드 자체가 없다(`candidateScores`는 길이 1로 정상 존재하는데
나머지 넷은 키 자체가 없다). `toFirestore()`의 `if (candidateFormalityScores
.isNotEmpty)` 가드는 후보가 1건이라도 평가되면(=`candidateScores`가
비어 있지 않으면) 같은 조건으로 항상 함께 채워지도록 설계했으므로
(§4 작업1 — 같은 반복문 안에서 같은 시점에 4개 배열에 동시에 `.add()`한다),
`candidateScores`만 있고 나머지가 없는 조합은 **신 코드가 실행됐다면
나올 수 없는 조합**이다.

**→ A는 실패다. 하나가 아니라 넷 다 비어 있으므로 배선이 안 된 것이
확실하다.** 3단계로 넘어가지 않는다.

### 원인 — 확정하지 않되, 유력한 가설 하나로 좁힌다

두 가지를 확인해 코드 쪽 결함 가능성을 먼저 배제했다:

- `addRecommendation`(`firestore_service.dart:297-304`)은 `entry
  .toFirestore()`를 그대로 `add()`에 넘긴다 — 필드를 수동으로 다시
  나열하는 별도 경로가 없다. `toFirestore()`가 새 필드를 이미
  담고 있고(git diff로 재확인), 쓰기 경로 자체에 누락 지점이 없다.
- `flutter analyze`/`flutter test`가 커밋 시점에 이미 통과했다(§4).

**남는 설명은 클라이언트 빌드다.** 계측을 배선한 커밋(`3f13bac`)의
시각은 **2026-08-11 15:47:25 KST**이고, 5건의 생성 시각은 같은 날
**15:50:03~15:54:24 KST** — 커밋 이후 **2분 38초 만에 첫 문서가
생성됐다.** 이 저장소의 실측 빌드 소요(`docs/task_hardening_v2.md`
§2, `flutter build apk --release` 약 2분 30초 + 설치)를 감안하면,
커밋 직후 그 짧은 시간 안에 재빌드·재설치까지 마치고 테스트를
시작했다고 보기 어렵다. **가장 유력한 설명은 이번 5건이 커밋
`3f13bac` 이전에 설치된 빌드로 생성됐다는 것이다** — 이 트랙이
여러 번 등록한 "재빌드 절차 결함"(논문 5.20.6/5.21.7절)과 같은
형태다.

**다만 이것은 가설이다, 확정이 아니다.** 확정하려면 이번 5건을
만든 기기의 설치 시각·앱 버전(빌드에 새긴 커밋 해시)을 직접
대조해야 하며, 그건 실기기를 쥔 사용자만 할 수 있다. **다음 단계**:
커밋 `3f13bac` 이후 재빌드·재설치 확인 → 같은 방식으로 5건(또는
그 이상)을 다시 생성 → 이 절차(A)를 재실행. 그 전까지 §4의 "완료"
표시는 "코드는 커밋됐다"는 뜻이지 "실기기에 배포됐다"는 뜻이
아니라는 점을 명시해 둔다.

### B) 5건 관측 기록 (총점만 — 축 데이터는 위 사유로 없음)

| 순서 | doc | 총점 |
|---|---|---|
| 1 | `7k4biDiE4mOU4HlsxjQX` | 82 |
| 2 | `FVp630ZOuZzUcSh2gTfv` | 82 |
| 3 | `U6lin7MpBa8ageOH8zD6` | 82 |
| 4 | `5bnjjJbAoU6SRYQDhDIe` | 75 |
| 5 | `KDVGUHNgkRhhyqIJf1jI` | 88 |

5건 전부 `threshold=70` 이상이며, 전부 후보 1개만 평가되고(`candidateScores`
길이 1) 통과해 수리가 발동하지 않았다. **해석하지 않는다** — "의도적으로
안 맞게 골랐는데도 다 통과했다"는 서술은 "안 맞음"이라는 사람 판단과
척도의 일치 여부를 전제하는데, 그 일치 여부 자체가 3단계(사람 라벨
상관)의 측정 대상이다. 지금 이 관측을 B-1의 (나) 근거로 쓰면 측정하기
전에 측정 대상을 결론으로 미리 쓰는 순환이 된다.

**표본 누적 관점만 기록한다**: 기존 66건 + 이번 5건 = 71건 중
`threshold` 미달 1건(1.4%) — 1단계의 1.5%(1/66)와 사실상 같은 비율로
유지되었다.

### C) 총점-축 관계 1차 관측 — 불가

표본 5로 총점과 세 축의 관계(평균/최솟값/합 중 무엇에 가까운지, 세
축이 전부 높은데 총점이 낮은 사례, 축 간 분산)를 보려 했으나 **볼
데이터 자체가 없다** — 위 A에서 확인했듯 이번 5건에는 축 점수가
아예 저장되지 않았다. 이 항목은 위 A가 해소된 뒤(재빌드·재설치 후
재생성) 다시 시도한다.

### 배선 검증 자체에 대한 반성

이번 실패는 "계측을 배선하고 그것이 실제로 도는지 확인하는" 단계에서
났다 — `task_hardening_v2.md` §3-3 축2가 등록한 원칙("고친 뒤의
침묵은 성공과 미배선을 구분해주지 않는다")의 사촌이다.

다만 이번엔 **침묵이 아니라 필드 부재**로 드러났다는 점에서 오히려
운이 좋았다. 만약 새 필드 4종이 기본값(빈 배열이나 `0`)으로
채워지는 구조였다면, A의 결과표는 `candidateFormalityScores=[]`나
`[0]` 같은 "값은 있는데 의미가 없는" 형태로 나왔을 것이고, 이는
"배선은 됐는데 이번엔 하필 파싱이 다 실패했다"는 **잘못된 설명과
구분되지 않았을 것이다.** 실제로는 키가 Firestore 문서에 아예
없었기 때문에("없음"과 "0/빈 배열"은 Firestore 조회에서 명확히
다른 신호다) 배선 여부를 즉시, 모호함 없이 판정할 수 있었다.

**부재가 기본값보다 진단에 유리하다** — 계측을 새로 배선할 때,
"값이 없으면 기본값으로 채운다"보다 "값이 없으면 필드 자체를
쓰지 않는다"(`toFirestore()`의 `if (list.isNotEmpty) '...'` 가드가
이미 이 형태였다)는 설계가, 배선 여부 자체를 진단해야 하는 순간에
더 정직한 신호를 낸다는 것을 이번이 실증했다.

### D) 3단계 착수 — 보류

A가 실패했으므로 이번 트랙에서 3단계 설계에 착수하지 않는다. 지시서가
사전에 정한 조건("A가 통과하면 3단계 설계를 사전 등록한다")을 그대로
지킨다 — A가 지금 통과하지 않았다고 조건을 완화하지 않는다. 반복
일관성·사람 라벨 설계에 필요한 사전 정보(자연 발생 반복 편차 7·3·0,
실효 범위 65~96/SD 7.1)는 §5에 이미 등록되어 있으므로, A가 해소되면
그 설계를 바로 이어서 시작할 수 있다.

## 4-2. 재빌드·재설치 (완료) — 새 옷 등록 재관측 대기

§0 정정에서 승계 목록에 추가한 절차를 여기서 처음 적용한다 — 실기기
조작을 사용자에게 요청하기 **전에** 설치 시각/대상 커밋 해시부터
기록한다.

- **대상 커밋**: `419db66`(§4-1 작성 시점의 `HEAD`) — 빌드 시작 시점에
  `git status`가 clean함을 확인했다(작업 트리에 이 문서 편집 외 코드
  변경 없음). 즉 빌드에 실린 소스는 정확히 `419db66`이다.
- **APK 빌드**: 시작 2026-08-11T07:01:34Z(=16:01:34 KST), 완료
  2026-08-11T07:05:02Z(=16:05:02 KST) — `flutter build apk --release`,
  `app-release.apk` 136.8MB.
- **설치**: `adb install -r`(**install -r만** — 삭제 후 재설치 아님,
  익명 uid·WorkManager 예약 보존). 기기를 처음 조회했을 때
  `adb devices`에 연결된 기기가 없어(`no devices/emulators found`)
  사용자에게 USB 연결을 요청했고, 재연결 확인(`R3CW10DF8CW`) 후
  완료 2026-08-11T07:06:05Z(=16:06:05 KST).
- **대조**: 설치 시각(16:06:05 KST)이 대상 커밋(`419db66`, 계측 배선
  커밋 `3f13bac`의 다음 커밋) 이후이며, 빌드가 그 커밋의 clean한
  작업 트리에서 나왔음을 직접 확인했다 — §0이 요구하는 대조를
  이번엔 재현했다.

**다음 단계(사용자 조작 필요)**: 이 기기에서 새 옷을 1벌 등록한다
(백그라운드 자기 평가 파이프라인이 발화하는 경로,
`agent_planner.dart:859`의 `enableRepair=true` 경로 — §4-1이
확인하려던 4개 필드가 실제로 채워지는지 볼 대상). 등록 후 §4-1의
A) 저장 확인 절차를 다시 수행한다.

## 4-3. §4-1 저장 확인 재수행 (2차, 2026-08-11) — 결과: 배선 통과

§4-2의 재빌드·재설치(커밋 `419db66`, 설치 07:06:05 UTC) 이후 사용자가
옷 3벌을 등록했다. 같은 서비스 계정 키로 다시 조회하고, 설치 시각
이후 생성된 문서만 대상으로 §4-1의 5건(계측 이전 빌드, 06:50~06:54
UTC)과 시각으로 분리했다.

### A) 저장 확인

| doc | createdAt(UTC) | 신 필드 4종 |
|---|---|---|
| `hrUoBgo7hD9JnKKU9lMe` | 07:09:05 | 전부 존재 |
| `U9N7eHNc3hZFfMAtaYwa` | 07:09:25 | 전부 존재 |
| `qtks4nrUC0e6PmQCmB6p` | 07:09:48 | 전부 존재 |

1. **후보별 축 점수 3종** — 존재한다. 세 문서 모두
   `candidateFormalityScores`/`candidateColorHarmonyScores`/
   `candidateStyleScores`가 `candidateScores`와 같은 길이(1, 세
   이벤트 전부 후보 1개가 즉시 통과)로 채워졌다. 이번 3건은 후보가
   각 1개뿐이라 "승자만이 아니라 후보 전부"가 검증됐다고 말하려면
   후보가 2개 이상인 이벤트가 더 필요하다 — 다만 코드 경로(§4 작업1,
   `.add()`가 후보 루프 안에서 매번 실행됨)는 이미 확인했으므로,
   이번 결과는 "그 경로가 실기기에서도 실제로 실행된다"를 보여줄
   뿐, 후보 2개 이상 케이스는 아직 실측되지 않았다.
2. **후보별 응답 모델** — 존재한다. 세 문서 전부
   `candidateModels=['gemini-3.5-flash']` — 기본 모델이 응답했고
   폴백(재시도로 인한 모델 전환)은 발동하지 않았다.
3. **총점과 축이 같은 문서에** — 그렇다. 세 문서 전부
   `colorScore`(총점)와 세 축이 함께 존재한다.
4. **파싱 실패 sentinel(0)** — 이번에도 확인 불가. 세 문서 모두
   축·총점 파싱이 전부 성공해 `0`이 한 번도 나오지 않았다.
   **"확인했는데 없다"가 아니라 "확인할 기회 자체가 없었다"** — 통일된
   sentinel(§4 작업2)이 실제로 그 값을 쓰는 장면은 아직 관측하지
   못했다.

**→ A는 통과다.** 신 필드 4종이 필드 부재가 아니라 실제 값으로
채워졌다 — §4-1의 1차 판단(배선 미반영)이 배선 이전 빌드를 본
결과였다는 §4-2의 가설이 이번 재검증으로 확정되었다. §4-2의
"배선 검증 자체에 대한 반성"(부재/기본값 구분)이 이번에도 그대로
적용된다 — 이번엔 부재가 아니라 **실제 값**이 나왔으므로 배선이
됐다는 것을 모호함 없이 판정할 수 있었다.

### B) 관측 기록

| doc | 총점 | 격식적합 | 색상조화 | 스타일통일 | 모델 |
|---|---|---|---|---|---|
| `hrUoBgo7hD9JnKKU9lMe` | 83 | 95 | 90 | 75 | gemini-3.5-flash |
| `U9N7eHNc3hZFfMAtaYwa` | 85 | 95 | 90 | 80 | gemini-3.5-flash |
| `qtks4nrUC0e6PmQCmB6p` | 82 | 95 | 90 | 75 | gemini-3.5-flash |

세 이벤트는 옷장 아이템 3종(`eX4cHUhrSMhv5GIGaB5Z`,
`Q2TqinzkRVKNgDoQE5dp`, `kC0gwb3bHizwF5TPp2Th`)을 공유하고 각각
새로 등록한 아이템 1개만 다르다 — 같은 "새 옷 등록" 세션에서
등록한 3벌이 각각 후보 매칭을 한 번씩 돌린 결과로 보인다. 셋 다
`threshold`(70) 이상, 후보 1개로 즉시 통과, 수리 미발동.

**누적 집계 갱신**: 기존 71건(1단계 66건 + §4-1의 5건, 모두 배선
이전 빌드 포함) + 이번 3건 = **74건 중 threshold 미달 1건(1.35%)**
— 1단계의 1.5%, §4-1의 1.4%와 사실상 같은 비율로 유지된다.

### C) 총점-축 관계 1차 관측 (표본 규명 — 아래 참고, 확정 아님)

**아래는 문서 3건의 1차 관측이며 확정이 아니다.** 다만 "표본 3"이라고
쓰지 않는다 — 아래("격식 95·색상 90 동일 원인 규명")에서 확인하듯
이 3건은 아이템 4개 중 3개를 공유하는 독립적이지 않은 표본이라,
**실질 표본은 3보다 작다**(아이템 겹침이 총점-축 내부 일관성에도
같은 방식으로 영향을 줄 수 있으므로, 아래 1·2번 관측도 3번 관측과
같은 제약을 받는다). §6-3에서 표본 1의 주관 평가가 다음 관측에서
재현되지 않은 전례가 있고, 여기서는 그보다도 더 좁은 실질 표본에
같은 위험이 적용된다.

1. **총점이 평균/최솟값/합 중 무엇에 가까운가**: 세 문서 모두 총점이
   축 평균보다 살짝 낮다 — (95,90,75)→평균 86.7, 총점 83(−3.7);
   (95,90,80)→평균 88.3, 총점 85(−3.3); (95,90,75)→평균 86.7,
   총점 82(−4.7). 최솟값(75/80/75)보다는 뚜렷이 높고, 합(260/265/260)과는
   스케일 자체가 다르다. **평균에 가장 가깝되 일관되게 3.3~4.7점
   낮다** — 세 표본뿐이라 이 오프셋이 우연인지 패턴인지는 판단하지
   않는다.
2. **불일치 사례(세 축 전부 높은데 총점 낮음, 또는 반대)**: 이번
   3건에서는 없다 — 축이 대체로 높으면(75~95) 총점도 대체로
   높다(82~85). 다만 표본이 전부 "합격" 구간에 몰려 있어(전부
   threshold 이상), 축이 낮은 상황에서 총점이 어떻게 움직이는지는
   이번 표본으로 알 수 없다.
3. **축 간 분산 — 이번이 핵심**: 세 문서 모두 **격식적합(95) >
   색상조화(90) > 스타일통일(75~80)** 순서가 그대로 반복됐고,
   격식-스타일 격차가 15~20점으로 작지 않다. 논문 286행이 서술하는
   "최저 점수 축을 원인으로 진단한다"는 절차가 이번 3건에서는
   **노이즈로 결정되지 않았다** — 스타일통일이 매번 뚜렷한 차이로
   최저였다. 다만 격식적합·색상조화 두 값이 세 문서에서 각각
   정확히 95·90으로 **완전히 동일**하다는 점도 함께 적어 둔다 —
   공유 아이템 3종이 같아서(§B) 모델이 그 두 축을 사실상 같은
   입력으로 취급했을 가능성과, 애초에 그 두 축의 변별력 자체가
   낮을 가능성을 구분하지 못한다. **B-3(총점과 축의 관계)과는 다른
   각도의 타당도 문제**(축들끼리의 변별력)이며, 이번 결과는 "노이즈로
   결정된다"는 우려 방향에 대해 3건 한정으로는 **반증**에 가깝지만,
   표본 3으로 이 절차의 타당성을 확정하지 않는다.

### 격식 95·색상 90 동일 원인 규명 (2026-08-11, 기존 데이터로 즉시 해소)

위 3번의 "격식적합·색상조화가 세 문서에서 완전히 동일한 이유"를
`itemIds` 집합 비교(순서 무관)로 확인했다.

- **후보 수**: 세 문서 모두 `candidateScores` 길이 1 — 세 이벤트
  전부 후보를 1개만 평가하고 즉시 통과했다. **같은 이벤트 안에서
  조합이 다른 후보끼리 축을 비교하는 것(아이템 공유 교란이 없는
  비교)은 이번 3건으로는 불가능하다** — 후보가 2개 이상인 이벤트가
  나와야 시도할 수 있다.
- **itemIds 겹침**: 세 문서가 아이템 4개 중 **3개를 공유한다**
  (`eX4cHUhrSMhv5GIGaB5Z`, `Q2TqinzkRVKNgDoQE5dp`, `kC0gwb3bHizwF5TPp2Th`
  — 세 문서 전부 동일). 다른 아이템 1개만 이벤트마다 바뀐다
  (`qHOrP1ECMCs7y6Ipxp9W`/`VxUaNvKWJcbYCLhHfCsq`/`UKvHI9gmwg4809IhzNJH`,
  아마도 새로 등록한 옷 자체). 겹침 비율 75%.

**결론: "겹치는 아이템이 많다" 쪽이다.** 격식·색상 축이 서로 다른
조합을 같은 값으로 잘못 뭉갠 것이 아니라, 실제로 조합의 3/4가
동일했으므로 그 두 축이 같은 값을 낸 것은 **정상이다** — 축이
아이템을 반영한 결과라는 설명이 데이터와 맞는다. 세 축의 변별력이
스타일 하나에 몰려 있다는 우려(후자 쪽 가설)는 이번 데이터로는
성립하지 않는다 — 다만 이는 "성립하지 않는다"이지 "반증됐다"가
아니다. 아이템이 겹치지 않는 표본에서 이 비교를 다시 해야 실제로
가른 것이 된다.

**실질 표본 크기 정정**: 이 3건은 독립 표본이 아니다 — 4개 아이템
중 3개를 공유하는 사실상 "같은 기저 조합 + 아이템 1개 교체"의
반복이다. 그래서 위 관측을 "표본 3"으로 표기하지 않는다 — **실질
표본은 3보다 작다**(기저 조합 1개 + 그 위에서 바뀐 아이템 1개씩
3가지 변주, 정도로 보는 것이 정확하다). §6-3이 "실행마다 아이템
집합이 달라 비교가 성립하지 않는다"를 교란으로 등록했다면, 이번은
**그 반대 방향** — 아이템 집합이 너무 같아서 독립 표본처럼 보이는
착시가 생긴다. 다음 3건 이상의 비교는 서로 다른 기저 조합에서
나와야 이 착시가 반복되지 않는다.

### 총점-축 편차 추적 — 고정 지표와 판정 기준 (2026-08-11 등록)

B-3의 "코드상 독립"을 "모델 출력도 무관하다"로 확대 해석하지 않기
위해, 앞으로 매 관측(§4-3류 저장 확인, 3단계 반복 측정 포함)마다
아래 네 값을 고정으로 함께 기록한다 — 계산식·표기를 지금 고정해
관측할 때마다 다시 정의하지 않는다.

- **(총점 − 축 평균)**: 부호와 크기.
- **(총점 − 축 최솟값)**, **(총점 − 축 최댓값)**.
- **축 3종의 표준편차**(모집단 표준편차, `n`분모): 축 간 변별력 —
  0에 가까우면 세 축이 사실상 같은 값을 내고 있다는 뜻이고, 그러면
  "최저 축을 원인으로 진단"이 노이즈로 결정될 위험이 커진다(위
  "격식 95·색상 90" 절과 같은 질문을 매 이벤트마다 정량화한 것).

**판정 기준(데이터를 보기 전에 고정)**: **독립 이벤트**(§ 위
"실질 표본 크기 정정" 절의 기준대로, 서로 아이템 과반이 겹치지
않는 이벤트만 센다) **20건 이상**이 모였을 때,

- (총점 − 축 평균)의 부호가 **18/20 이상** 한쪽으로 몰리면 —
  "총점과 축 사이에 모델 출력 수준의 규칙성이 있다"로 판정하고
  B-3에 정정 블록을 추가한다(코드 관계는 여전히 없다는 서술은
  유지하되, 출력 수준의 경험적 관계를 별도로 기록한다).
- 20건 미만이거나 부호가 갈리면(예: 12/20 같은) — 판정을 보류하고
  계속 관측한다. **지금 이 시점(독립 표본 사실상 0~1건)에서는
  판정하지 않는다.**

20건이라는 문턱은 검정력 계산이 아니라 실무적 선택이다 — 자연
발생·의도적 등록으로 이 정도 표본이 몇 주 안에 쌓일 것으로 보이며,
18/20(90%)은 자연 발생 반복 3쌍에서 이미 관측된 편차(7·3·0, 부호가
전부 관측 방향과 일치하지는 않을 수 있다는 점까지 감안)보다 뚜렷한
쏠림을 요구하는 수준으로 잡았다. 논문 5.16절이 "반복 횟수는 비용이
허락하는 한 최소 3회, 임계값 판정에는 5회를 기준으로 삼는다"고
실무적 문턱을 명시한 것과 같은 종류의 선택이다.

## 5. 3단계 예고 — 등록만, 설계는 아직 하지 않는다

아래는 착수 예고이며 사전 등록 문서가 아니다. 계측(§4)이 배포되고
데이터가 쌓이기 시작한 뒤, 판정 기준을 라벨·측정 착수 **이전에** 서면
고정한 별도 절을 이 문서에 추가한다.

- **반복 일관성**: 새 옷 등록 경로(`agent_planner.dart:859`, 이력
  미포함 — §2에서 확인한 "입력이 조합만으로 결정되는" vehicle)를 쓴다.
  자연 발생 반복(1단계 조사 중 itemIds 조인으로 발견한, 같은 계정·같은
  TPO·수 초 간격의 단일-후보 재평가 3쌍)에서 이미 편차 7·3·0점이
  관측되었다 — 표본 크기 산정의 사전 정보로 쓴다. 나머지 반복(다른
  TPO 태그 간 재출현)은 `recentHistoryText`가 달랐을 수 있어(§2 피드백
  루프) "같은 조합·다른 맥락"으로 구분해 별도 취급한다.
- **사람 라벨과의 상관**: 개정 6판(임베딩 회수 검증)의 블라인드
  쌍대비교 절차를 재사용한다. B-1의 (가)/(나)가 여기서 갈린다 — 사람
  라벨이 68~96 범위에 고르게 분포하면서 모델 총점과 상관이 낮다면
  (나) 쪽, 사람도 비슷하게 후하게 매기면 (가) 쪽에 무게가 실린다.

### D) 3단계 착수 조건 정리 (등록만, 설계 아님 — 2026-08-11)

§4-3에서 A(배선 검증)가 통과했으므로 3단계 설계에 필요한 선행 정보를
정리한다. **아래는 설계가 아니라 설계를 쓸 때 갖고 들어갈 재료다** —
판정 기준(표본 크기, 유의 수준, 승패 문턱)은 라벨링 착수 이전에
별도 절로 서면 고정한다(§0 승계 원칙).

**반복 일관성 — 표본 크기 산정에 쓸 사전 정보**:
- 자연 발생 반복(1단계, itemIds 조인)에서 관측된 편차: 7·3·0점
  (3쌍, 근접 조건).
- 실효 범위 65~96(SD 7.1, §3-1) — 표본 크기 계산은 명목 1~100이
  아니라 이 범위·이 분산을 기준으로 한다(B-2).
- vehicle은 새 옷 등록 경로(`agent_planner.dart:859`, 이력 미포함,
  §2) — 입력이 조합만으로 결정되어 반복 시 `recentHistoryText`
  드리프트가 없다.

**사람 라벨 — 왜 절대 판단이 아니라 쌍대 선호여야 하는가**:
이 트랙이 확인하려는 것은 정확히 "1~100 척도가 이 문제에서 의미
있는 눈금인가"다(B-2). 사람에게 "이 조합 몇 점?"을 물으면 **같은
질문을 사람에게 그대로 떠넘기는 것**이 된다 — 사람도 100점 척도
위에서 안정된 절대 기준을 갖고 있다는 보장이 없고, 그 기준 자체가
검증 대상과 같은 종류의 불확실성을 갖는다. 반면 "A와 B 중 어느
쪽이 더 잘 어울리는가"는 척도 없이 답할 수 있는 질문이다 — 정확히
개정 6판이 두 정책의 산출물을 비교할 때 쓴 것과 같은 형태의 판단
(절대 품질이 아니라 상대 선호)이며, 그래서 그 절차를 재사용할 수
있다.

**개정 6판 절차에서 재사용할 부분 / 바꿔야 할 부분**:

| | 개정 6판(정책 비교) | 이 트랙(척도 검증) |
|---|---|---|
| 그대로 쓴다 | 블라인드(라벨러가 무엇을 보는지 모름), 좌우·제시 순서 무작위, 단일 세션 완주, 판정 기준 사전 등록·중단 규칙 | 좌동 |
| 바꿔야 한다 | 쌍의 구성 — 두 정책이 갈리는 discordant 쌍(같은 맥락, 정책만 다름) | 쌍의 구성 — **같은 맥락 비교가 아니다.** 모델 총점 범위(65~96)에
전 구간을 걸치도록 조합을 층화 추출해 쌍을 만든다(예: 상위/중위/
하위 구간에서 각각 뽑아 짝짓는다) — 비교 대상은 정책이 아니라
점수 그 자체이므로 짝짓는 논리가 다르다 |
| 바꿔야 한다 | 판정 통계 — 이항검정(승/패, 사전 등록한 문턱과 대조) | 판정 통계 — 사람 선호 순서와 모델 점수 순서의 **일치도**(순위
상관 계열)를 본다. 두 정책 중 하나를 고르는 이항 판정이 아니라
연속값 순서에 사람이 동의하는 정도를 재는 문제이기 때문이다 —
통계 선택 자체는 이 절에서 확정하지 않는다, 3단계 설계에서 함께
고정한다 |

두 설계(반복 일관성·사람 라벨) 모두 라벨·측정 착수 이전에 판정
기준을 서면 고정한다 — §0의 승계 원칙 그대로다.

## 6. 3단계 사전 등록 (2026-08-11, 착수 조건 A·B 정리 완료 후)

§4-3에서 A(배선 검증)가 통과했고, 위 §4-3 "총점-축 편차 추적"에서
B(판정 기준)를 먼저 고정해 두었으므로, 지시서의 조건("A·B가
정리되면 3단계 설계를 사전 등록한다")에 따라 설계를 쓴다. **아직
착수(라벨링·반복 호출 실행)하지 않는다** — 이 절은 등록이지 실행
기록이 아니다.

### 6-가. 반복 일관성

**목적**: 같은 입력(같은 조합, 이력 없음)을 반복 평가했을 때 총점의
변동 폭을 잰다.

**방법**: `agent_planner.dart:859`의 새 옷 등록 경로를 실기기 UI로
20회 반복하는 것은 매번 실제 옷장에 새 아이템을 추가해야 해서
비현실적이다(옷장이 오염되고, 매번 다른 조합이 되어 "같은 입력"이
성립하지 않는다). 대신 self-eval이 실제로 보내는 것과 동일한 함수
(`GeminiService.analyzeOutfitFromAttributes`, 이력 없음,
`temperature=0.7`, 기본 모델 `gemini-3.5-flash`)를 **고정된 조합
하나**로 반복 호출하는 측정 전용 하네스를 쓴다 — 서버 프록시
(`callGeminiText`)를 그대로 거치되, 앱 UI나 Firestore
`recommendations` 쓰기는 거치지 않는다(측정 결과를 실사용 데이터에
섞지 않는다).

**대상 조합**: 이미 옷장에 등록된 실제 아이템 3~4벌 조합 1개를
고정한다 — **관측 총점이 threshold(70) 근방(65~75)에 오는 조합을
우선 고른다.** 신뢰도가 실무적으로 문제가 되는 지점은 정확히 그
경계이기 때문이다(반복 시 점수가 문턱을 넘나들면 같은 조합이 어느
날은 통과, 어느 날은 미달로 갈린다 — B-1의 (나) 가설이 실제로
결과에 영향을 주는 지점). 경계에서 먼 조합(예: 항상 90점대)의
변동은 실무적 함의가 작다.

**표본 크기**: N=20 반복. 검정력 계산이 아니라 실무적 선택이다 —
자연 발생 반복 편차(7·3·0, §1)로 신뢰구간을 추정하기엔 3쌍은 너무
작고, §4-3이 이미 등록한 "독립 이벤트 20건" 문턱과 이 트랙 안에서
표본 크기 관행을 맞춘다.

**기록할 값**: 20개 총점의 평균·SD·range(max−min), 그리고 20회 중
판정(pass/fail)이 갈리는 횟수·비율.

**판정 기준(데이터를 보기 전에 고정)**:
- SD ≥ 5점(자연 발생 최대 편차 7점의 약 70%를 여유 기준으로 잡음)
  이거나 20회 중 판정이 1회 이상 갈리면 → **"신뢰도 우려"**로
  등록한다. 즉시 대응(예: 반복 평가 후 평균으로 채택 여부를 정하는
  방향으로의 설계 변경)하지 않되, 근거로 남긴다.
- SD < 5점이고 20회 전부 같은 판정 → **"이 조합에서는 신뢰도가
  충분"**으로 잠정 결론한다. 조합 1개로 일반화하지 않는다 — 다른
  조합에서 반복하는 것은 이 문서 범위 밖(4단계 이후 과제)으로
  명시해 둔다.

### 6-나. 사람 라벨 (쌍대 선호)

**목적**: B-1의 (가)/(나)를 가른다 — 모델 총점 순서가 사람의 선호
순서와 일치하는지.

**표본 구성**: 누적 Firestore 데이터(§4-3까지 74건)에서 총점이
넓게 퍼지도록 층화 추출한다 — 하위(65~74)·중위(75~84)·상위(85~96)
세 구간에서 각각 뽑아, 구간을 넘나드는 쌍(격차가 큰 비교)과 같은
구간 안의 쌍(격차가 작을 때도 사람이 갈라내는지 보는 대조)을 섞는다.

**표본 크기**: 목표 15~20쌍(개정 6판의 discordant 21쌍과 같은
규모). **정확한 숫자는 미확정으로 남긴다** — 사람이 눈으로 비교할
수 있는 형태로 조합을 재구성하려면 그 시점 옷장의 아이템 이미지·
설명이 남아 있어야 하는데, 이걸 아직 확인하지 않았다. 이 확인
항목 자체를 여기 등록해 둔다 — 3단계 착수 시점에 "몇 쌍이 가능한지"
를 다시 조사하는 게 아니라 이미 알려진 확인 항목을 처리하는 것으로
만들기 위해서다.

**제시 형식**: 블라인드(라벨러는 어느 쪽 총점이 높은지 모른다),
좌우 배치·제시 순서 무작위, 단일 세션 완주 — 개정 6판 절차 그대로
(§5 표 "그대로 쓴다" 행).

**질문 형식**: "이 두 조합 중 어느 쪽이 더 잘 어울리는가" — 절대
점수가 아니라 상대 선호(§5에서 이미 등록한 근거: 절대 점수를
물으면 검증 대상과 같은 종류의 척도 문제를 사람에게 떠넘기게 된다).

**판정 통계(데이터를 보기 전에 고정)**: 사람 선호가 모델 총점
순서와 일치하는 쌍의 비율을 이항검정한다 — 무작위 기대치 50% 대비,
15쌍 기준 11/15(73%) 이상 일치하면 유의 수준 5% 근방에서 "상관
있음"((가) 쪽에 무게)으로, 50%에 가깝게 갈리면(예: 8/15 근방)
"상관 없음"((나) 쪽에 무게)으로 등록한다. 정확한 문턱·p값은 최종
표본 크기가 확정된 뒤 재계산한다 — 이 이항검정 관행 자체는 개정
6판의 것을 재사용한다.

**중단 규칙**: 개정 6판과 동일 — "설명 가능하니 진행"을 배제한다.
라벨링 도중 같은 쌍에서 판단 불가가 반복되면(3회 이상) 그 쌍
구성을 재검토한다.

### 미확정으로 남기는 것

라벨러가 누구인지(개정 6판처럼 이 트랙 작업자 본인인지), 반복
일관성 하네스를 어디에 둘지(저장소 안 `tools/`인지 스크래치패드
전용인지)는 아직 정하지 않았다 — 착수 직전에 정하고, 정한 근거를
이 절에 추가한다. **착수하기 전까지는 이 절의 숫자·기준을 결과를
핑계로 바꾸지 않는다** — §0의 승계 원칙.

## 부록 — 실행 기록

- **1단계 조회**: 2026-08-11, `ai-fashion-assistant-personal`, 읽기
  전용, `tools/score_distribution_report/score_distribution_report.py`
  (수정 없이 그대로 실행) + 보조 스크립트(itemIds 조인, 저장소 밖
  스크래치패드에 위치, 저장소에 커밋하지 않음). 서비스 계정 키는
  저장소 밖 로컬 경로(`~/fb-keys/`) 사용.
- **2단계 코드 변경**: `lib/services/outfit_self_evaluator.dart`,
  `lib/models/recommendation_entry.dart`, `lib/services/agent_planner.dart`,
  `lib/services/firestore_service.dart`(주석만).
- **2단계 저장 확인(1차, 배선 이전 빌드로 판명)**: 2026-08-11, 같은
  자격 증명, `verify_axis_wiring.py`(보조 스크립트, 저장소 밖).
- **재빌드·재설치**: 커밋 `419db66`, 빌드 07:01:34~07:05:02Z, 설치
  07:06:05Z(§4-2).
- **2단계 저장 확인(2차, 배선 통과 확인)**: 2026-08-11, 같은 스크립트·
  자격 증명, 설치 시각 이후 문서만 대상(§4-3).
