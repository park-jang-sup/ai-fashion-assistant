# DOT 프로젝트 인수인계 — 2026-08-15 기준

이 문서는 새 대화에서 작업을 이어받기 위한 것이다. **처음 읽는 순서**:
§1(요약) → §2(미해결 항목 표) → §3(sweepStorageTokens 원인 규명)
순으로 읽으면 착수 가능하다.

**작업 원칙·기본 정보는 이 문서에 다시 옮겨 적지 않는다** —
`docs/handoff_2026-08-07.md` §0·§2를 그대로 참조한다(승계 원칙,
판정 기준 사전 등록, 계측 우선, 원문 보존 + 정정 블록 등). 다만
§0의 "논문: `DOT_paper_rev7.md` — 저장소 밖 별도 파일" 서술은
낡았다 — 지금은 `docs/DOT_paper_rev9.md`가 저장소 안에 있고
유일한 판본이다(rev8 이하는 삭제, git 이력에는 남음).

## 0. 이 문서가 생긴 경위

이 표는 **rev8 삭제 여부를 판단하는 무관한 작업에서 우연히
시작된 감사**(논문 6.5.7절 참고)가 계기가 되어, "여러 트랙에
흩어진 미해결 항목을 한 곳에 모아 시연 전/나중을 가른다"는
별도 작업으로 이어진 것이다. 정기 감사로 제도화할지는 아직
정하지 않았다.

## 1. 요약

- 미해결 항목을 전수 조사한 결과, **앱 동작을 막거나 데이터를
  잃는 항목은 없다.** 대부분 실사용자 유입 시에만 발동하거나
  조건 대기 중이다.
- **시연 전에 확인해 두는 게 안전한 것 셋**을 §2 표에 표시로
  구분해 뒀다 — 컷아웃 소실 15건, `fittingLimit`(6/시간) 소진,
  동시성 타협에 의한 중복 추천. 셋 다 "막는다"가 아니라 "낮은
  확률로 심사위원 눈에 보일 수 있다"는 성격이다.
- `sweepStorageTokens`(§3)는 원인이 규명됐고 **현재 정상
  작동 중**이다 — 죽어 있던 안전망이 아니라 재배포 빈도가
  높을 때만 일시적으로 밀리는 것으로 확인됐다.
- **이미 닫힌 항목은 이 표에 없다.** 작업 전 각 문서의 종결
  기록을 확인했다 — 예: S1·S3·S4·S5(hardening), App Check
  `activate()` 조치(fire-and-forget 전환), 발화 정책 자기 조정
  (a)(b) 전부(cadence, 서버 게이트 포함), 자기평가 라우팅 편향의
  판정·선택 층위(paper 5.23), 신규 아이템 실시간 임베딩의
  "보류" 판정(재조사 아님, 이미 내려진 결정) 등.

## 2. 미해결 항목 — 전체 표

**분류 기준**: "시연 전"은 앱이 동작하지 않거나 심사위원이
즉시 확인 가능한 불일치를 만들거나 데이터 소실 위험이 있는
것. "나중"은 실사용자 유입 시에만 발동하거나 조건 대기 중인
것.

| # | 항목 | 출처 | 현재 상태 | 보류/대기 조건 | 분류 |
|---|---|---|---|---|---|
| 1 | App Check 강제(S2-b) 미적용 — 파생: 익명 uid 상한 우회, Firestore/Storage 출처 미검증 | `task_hardening_v2.md` §3-1-3·§5, 논문 7.1·7.2-16 | 판정 후 보류(3건이 한 선결조건에 종속) | Play 경유 배포 확보 / 사이드로드 Play Integrity 실측 / 심사·시연 배포 방식 확정 — 셋 중 하나 | 나중(지금 강제하면 오히려 앱이 막힘) |
| 2 | 주간 코디 플랜 옷장 규모 간접 상한(30,000자) | `task_hardening_v2.md` §4-4, `task_weekly_plan_scale_v1.md`, 논문 5.24·7.1 | 실측 완료(벌당 95.24자, 도달 약 303벌), 조정 방향 미착수 | 다음 세션에서 (a)/(b) 방향 결정 | 나중(현재 옷장 규모가 상한의 절반 이하) |
| 3 | `sweepStorageTokens` 24시간 미발화 | `task_hardening_v2.md` §6-1 | **원인 규명 완료(§3 참고), 현재 정상 작동 확인됨** | — | 나중(재배포 빈도가 다시 높아지면 재발 가능 — 관찰만) |
| 4 | 준비 단계 안전망 부재(3중 안전망 밖 다운로드) | 논문 5.19.1·7.1, `task_hardening_v2.md` §6-2 | 2026-08-11 재발 관측 1회 비재현(표본 1, 미확정) | 자연 재발 대기 | 나중 |
| 5 | 시한 없는 호출(60초 시한이 5분+ 매달림) | 논문 5.19.2·7.1 | 원인 미규명, 재현 절차 없음 | 자연 재현 대기 | 나중 |
| 6 | 포그라운드 실패의 태스크 미생성 | 논문 5.19.3·7.1 | 미해결 | — | 나중 |
| 7 | 오프라인 등록 시 파일 없는 문서 | 논문 5.19.4·7.1 | 미해결, 검증 불가 | — | 나중 |
| 8 | **파일 9개 영구 소실 — 컷아웃 15건은 가상 피팅에서만 실패** | 논문 5.18.3·7.1 | 대체 원본 재등록 전까지 미해결 | 원본 재등록 | **시연 전 확인** — 이 15건 중 하나가 데모에서 선택되면 가상 피팅이 실제로 실패한다. 어떤 아이템인지 파악해 두거나 데모 동선에서 피할 것 |
| 9 | 가상 피팅 업스트림 회귀(옷 2벌+, 60~300초) | 논문 5.21·7.1 | 원인 미규명(업스트림 귀속, 조사 종결), 순차 합성으로 우회(노출 빈도만 감소) | — | 나중(우회책 배포·검증됨, 완전 배제는 아님 — 재현성 확인 중 76.2초 스파이크 1건) |
| 10 | 재시도가 API 비용 두 번 씀 | 논문 5.21.6·7.1 | 등록만, 조치 안 함 | — | 나중 |
| 11 | **`fittingLimit`(6/시간)이 시연에 충분한지 미관측** | 논문 7.1·7.2 | 값 유지, 관측 근거 없이 안 올림 | 실사용 체감 관측 | **시연 전 확인** — 시연 중 가상 피팅을 6회 넘게 반복하면 그 자리에서 상한 초과 에러가 뜬다. 리허설로 소진량을 미리 파악할 것 |
| 12 | 계획 생성·도구 선택 미착수(에이전트-워크플로우 경계 나머지 둘) | 논문 5.27.7·7.1·7.2-19 | 미착수, 연구 방향만 등록 | — | 나중 |
| 13 | 발송-탭 매칭 데이터 0건 | `task_agent_cadence_v1.md` §4-4, 논문 5.27·7.1 | 배선 완료, 자연 축적 대기 | uid당 매칭 30건 | 나중(조치 불필요, 시간이 채움) |
| 14 | 신규 아이템 실시간 임베딩 재개 | `task_embedding_impl_v1.md`, 논문 7.2-5 | **판정 확정 — 보류**(재조사 아님) | 상점 카탈로그 데이터 소스 확보 | 나중 |
| 15 | 자기평가 총점 이산성 / 폴백률 급등 원인 / L모델 75·85 편향 원인 | `task_selfeval_validity_v1.md` 종결부 | 미확정, 별도 트랙 필요 | 다음 자연 발생 실패 대기(인위 생성 금지) | 나중 |
| 16 | 익명 계정 5개 정리 | `task_selfeval_validity_v1.md` §6-가 "정리 필요" | 삭제 보류 중 | 사용자 확인 | 나중 |
| 17 | 조합 품질 개선 여지(색상 축, n=1 옷장) | 논문 7.1 "유지되는 한계" | 구조적, 다른 옷장 확보 전까지 불가 | 다른 옷장 확보(로드맵 16번) | 나중 |
| 18 | 검증 체계 비대칭(배선 계층 미검증) | 논문 7.1·7.2-6(오늘 정정) | 5.18 두 사고와 직결, 미해결 | — | 나중(개발 프로세스 리스크, 시연과 무관) |
| 19 | 재계획 전제 협소(일정 태그 변경 미감지) | 논문 7.1 "유지되는 한계" | 설계만 있고 미구현 | — | 나중 |
| 20 | 얕은 개인화(모델 자체 학습 없음) | 논문 7.1 "유지되는 한계" | 구조적 한계 | — | 나중 |
| 21 | **동시성 타협 — 같은 날짜 추천 2건 생성 가능** | 논문 7.1 "유지되는 한계" | 락이 트랜잭션이 아닌 읽기-후-쓰기, 중복 게이트가 사후 정리만 함 | — | **시연 전 인지** — 백그라운드와 앱 내 실행이 겹치면 낮은 확률로 같은 날짜에 추천이 두 개 보일 수 있다. 시연 직전엔 백그라운드 트리거와 겹치지 않게 할 것 |
| 22 | 재로그인 전용 UX 미착수 | 논문 5.24.4·7.1 | `unauthenticated`는 "일시적"로 분류만, 유도 화면 없음 | — | 나중 |
| 23 | 주간 플랜 실패 계측 관측치 없음 | 논문 5.24.4·7.1 | 배선·검증 완료, 자연 관측 대기 | — | 나중 |

**표에서 뺀 것 — 이미 닫힌 항목(재조사 안 함)**: S1·S3·S4·S5(하드닝),
App Check `activate()` fire-and-forget 전환, `SKIP_APPCHECK` 플래그
제거, 발화 정책 자기 조정 (a)(b) 전체(서버 게이트 포함), 자기평가
라우팅 편향 판정·선택 층위 완결, 주간 플랜 에러 문구 오분류,
Artifact Registry 우려, 예보 좌표 고정, 데모 옷장 진입점, 임계값
부등호 재검토(문제 없음으로 닫힘), 배경 제거(서버 이관 완료).

## 3. `sweepStorageTokens` 원인 규명 (2026-08-15)

**결론: 인프라·코드 결함이 아니다.** `every 24 hours`(기간 기반
스케줄)가 함수 재배포마다 기준 시각을 리셋하는데, 하드닝 트랙
자체의 잦은 재배포가 24시간이 채 지나기 전에 매번 리셋을 걸어
발화가 계속 밀려났다. 전체 근거·타임스탬프 대조는
`task_hardening_v2.md` §6-1 "원인 규명(2026-08-15)"에 원문으로
남겼다 — 여기서는 요지만 옮긴다.

- `firebase functions:list`로 스케줄 잡 생성 자체는 확인(무료).
- `firebase functions:log`의 `scanned=.. revoked=..` 라인 타임스탬프가
  결정적 증거였다: 재배포가 뜸했던 08-11~08-14 나흘은 매일 정확히
  24시간 간격으로 정상 발화했고, 08-09~08-10 재배포 밀집 구간(6회)
  에서는 매번 다음 재배포가 24시간 타이머보다 먼저 와 리셋됐다 —
  마지막 재배포로부터 정확히 24시간 후에야 처음 발화했다.
- 코드상 조기 반환 경로 없음, timeZone 옵션도 `scheduledProactiveCheck`
  와 동일(둘 다 미지정) — 이 두 축은 원인이 아니다.
- **오늘 이 세션도 같은 패턴을 다시 만들었다**(발화 정책 자기
  조정 트랙의 배포 2회). 다음 예상 발화는 마지막 배포로부터
  24시간 후(2026-08-16 약 10:24 UTC)이며, 그 전에 "0회"로 관측돼도
  이번엔 정상이다 — 같은 착시를 반복하지 않기 위해 명시해 둔다.
- 조치(절대 시각 크론으로 전환 등)는 후보만 등록하고 착수하지
  않았다 — 지시 범위가 원인 규명까지였다.
